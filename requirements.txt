# 지하철 혼잡 예측 모델 — 의존 패키지
# Python 3.11.9 기준, Render 무료 티어에서 검증된 안정 버전
#
# 설치: pip install -r requirements.txt
# 실행: uvicorn app.main:app --reload

# --- 데이터 처리 ---
# pandas 2.x + numpy 1.x 조합이 가장 안정적 (numpy 2.x / pandas 3.x 는 일부 환경에서 빌드 이슈)
pandas==2.2.3
numpy==1.26.4

# --- 머신러닝 (AI 보조 모델) ---
scikit-learn==1.5.2

# --- 시각화 ---
matplotlib==3.9.2

# --- 웹 서버 (FastAPI) ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
jinja2==3.1.4
python-multipart==0.0.12   # form 파싱용
