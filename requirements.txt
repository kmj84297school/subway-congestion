# 지하철 혼잡 예측 모델 — 의존 패키지
# Python 3.10 이상 권장
#
# 설치: pip install -r requirements.txt
# 실행: uvicorn app.main:app --reload

# --- 데이터 처리 ---
pandas>=2.0,<4.0
numpy>=1.24,<3.0

# --- 머신러닝 (AI 보조 모델) ---
scikit-learn>=1.3,<2.0

# --- 시각화 ---
matplotlib>=3.7,<4.0

# --- 웹 서버 (FastAPI) ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9   # form 파싱용
